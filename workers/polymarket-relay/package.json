{
  "name": "ost-poly-relay",
  "version": "1.0.0",
  "private": true,
  "description": "Read-only Polymarket Gamma + CLOB relay running on Cloudflare's global edge for low-latency arbitrage and OST prediction market data.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
